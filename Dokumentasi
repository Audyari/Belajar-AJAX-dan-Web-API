Melakukan Request Dengan Xmlhttprequest

=============================================

contoh seder hana penggunaan Xmlhttprequest pada web : https://icanhazdadjoke.com/

index.html :

<!DOCTYPE html>
<html>
<head>
  <title>XMLHttpRequest Example</title>
</head>
<body>
  <h1>XMLHttpRequest Example</h1>
  <button onclick="makeRequest()">Make Request</button>
  <div id="response"></div>

  <script>
  function makeRequest() {
  // Membuat objek XMLHttpRequest
  var xhr = new XMLHttpRequest();

  // Mendefinisikan metode HTTP dan URL tujuan
  xhr.open('GET', 'https://icanhazdadjoke.com/', true);

  // Mendefinisikan header request agar menerima response dalam format JSON
  xhr.setRequestHeader('Accept', 'application/json');

  // Mendefinisikan callback saat request selesai
  xhr.onreadystatechange = function() {
    if (xhr.readyState === XMLHttpRequest.DONE && xhr.status === 200) {
      // Request berhasil
      var data = JSON.parse(xhr.responseText);
      document.getElementById('response').textContent = JSON.stringify(data, null, 2);
    } else {
      // Request gagal
      document.getElementById('response').textContent = 'Error: ' + xhr.status;
    }
  };

  // Mengirim request
  xhr.send();

}
  </script>
</html>

===================================================
